# Contributing

Thanks for your interest in contributing! Short guidelines:

- Fork the repository and create feature branches from `main`.
- Run `npm install` then `npm run build` and `npm test` before opening a PR.
- Write tests for new behavior and keep changes small and focused.
- Use conventional commit messages if possible.

If you'd like help picking issues, open a discussion or ping via PR.
